import React, { ReactElement } from 'react'

interface Props {
    
}

function SignupModal({}: Props): ReactElement {
    return (
        <div>
            
        </div>
    )
}

export default SignupModal
